## 📁 Dataset Description 

### 📄 train.tsv / valid.tsv / test.tsv (LIAR Dataset)
The LIAR dataset contains short political and public claims labeled for truthfulness.  
It is used for **text-based misinformation detection**.  
- `train.tsv` → model training  
- `valid.tsv` → validation & threshold tuning  
- `test.tsv` → final evaluation  

Each entry includes a claim, its truth label, speaker, and context.

---

### 🖼️ real and fake/
This folder contains **image-based misinformation data**.  
Images are grouped into **REAL** and **FAKE** categories and used for:
- Image-only fake news detection  
- Multimodal (Text + Image) classification  

---

### 🧠 ifnd dataset/
The IFND (Image-based Fake News Dataset) contains news claims with associated metadata and labels.  
It is mainly used for:
- Evidence-based verification  
- Supporting multimodal reasoning  

---

### 📰 politi fact check/
This folder contains fact-checked claims from **PolitiFact**, a trusted fact-checking source.  
It is used as an **external evidence corpus** to:
- Retrieve supporting or contradicting evidence  
- Improve credibility scoring  

---

### ⚙️ processed/ (processed data set is used in the project)
It is **cleaned and preprocessed data** generated by our scripts.  
It includes:
- Normalized labels (Real = 1, Fake = 0)  
- Cleaned text  
- Mapped image paths  
- CSV files ready for model input  

This folder can be generated according to the user.

---

### 🔧 Dataset Processing Summary
- Text data was cleaned, tokenized, and label-normalized  
- Image datasets were converted into CSV mappings  
- Evidence datasets were indexed for retrieval  
- All datasets were unified into a **common label format** for fair evaluation  

